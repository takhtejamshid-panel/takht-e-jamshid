<div dir="rtl">

# 🔒 سیاست امنیتی

## نسخه‌های پشتیبانی‌شده

| نسخه | وضعیت |
|---|---|
| `0.1.x` | ✅ پشتیبانی می‌شود |
| قدیمی‌تر | ❌ پشتیبانی نمی‌شود |

---

## گزارش آسیب‌پذیری

اگر آسیب‌پذیری امنیتی پیدا کرده‌اید، لطفاً **Issue عمومی باز نکنید.**

از مسیر امن گیت‌هاب استفاده کنید:
**`Security → Report a vulnerability`** در صفحه‌ی مخزن.

اگر امکانش را ندارید، یک Issue با عنوان `[SECURITY]` باز کنید و **فقط**
توضیح کلی بدهید (بدون جزئیات بهره‌برداری) تا با شما تماس بگیریم.

### چه چیزی در گزارش بیاورید

- شرح مشکل و بخش مربوطه‌ی کد
- گام‌های بازتولید (تا جایی که بدون افشا ممکن است)
- نسخه‌ی پنل
- شدتِ تخمینی و تأثیر احتمالی

### زمان پاسخ‌گویی

- تأیید دریافت: حداکثر ۷۲ ساعت
- برآورد اولیه: حداکثر ۷ روز
- انتشار وصله: بسته به شدت، معمولاً ظرف ۱۴ روز

---

## مواردی که گزارش امنیتی محسوب **نمی‌شوند**

- مسیر پنل با نام پیش‌فرض (`takht`) تغییر نکرده باشد → این وظیفه‌ی نصب‌کننده است
- گذرواژه‌ی پیش‌فرض (`admin`) تغییر نکرده باشد → همین‌طور
- افشای لینک اشتراک توسط خودِ کاربر
- محدودیت‌های شناخته‌شده‌ی پلتفرم (مثل نبود UDP واقعی روی Workers)
- رفتارِ استتار (نمایش صفحه‌ی مرجع به‌جای پنل)

---

## توصیه‌های امنیتی برای نصب‌کنندگان

1. **بلافاصله پس از نصب** گذرواژه را تغییر دهید.
2. **مسیر مخفی** را از مقدار پیش‌فرض تغییر دهید و آن را نشانک کنید.
3. توکن اشتراک هر کاربر را مانند گذرواژه در نظر بگیرید.
4. دسترسی به حساب کلودفلر خود را با احراز هویت دومرحله‌ای محافظت کنید.
5. از پشتیبان‌گیری دوره‌ای (تب «پشتیبان») غافل نشوید.
6. پنل را روی دامنه‌ای منتشر نکنید که به‌راحتی به شما ردیابی شود.

---

## پایبندی‌های خودِ پروژه

- هیچ داده‌ای از کاربرانِ پنل جمع‌آوری یا ارسال نمی‌شود.
- هیچ تماس شبکه‌ایِ پنهان یا «تلفنِ خانه» در کد وجود ندارد.
- خروجی نهایی (`_worker.js`) از کد منبعِ همین مخزن ساخته می‌شود و
  در CI با `src/` مقایسه می‌گردد تا چیزی پنهان در آن نباشد.

</div>
