<div dir="rtl">

# 📜 تغییرات تخت جمشید

قالب این فایل بر پایه‌ی [Keep a Changelog](https://keepachangelog.com/fa/1.1.0/) است
و پروژه از [نوردهی معنایی](https://semver.org/lang/fa/) پیروی می‌کند.

## [0.2.0] — ۲۰۲۶-۰۸-۳۰

### افزوده‌شده

**اسکنر آی‌پی تمیز درون‌پنل (ماژول `src/11_scanner.js`)**
- نمونه‌برداری از ۱۵ بازه‌ی رسمی IPv4 کلودفلر، دو روشِ متوازن و تصادفیِ قابل‌تکرار
- اندازه‌گیریِ دیتاسنتر، کشور، نسخه‌ی TLS و تأخیر از لبه با `cf.resolveOverride`
- همزمانی و مهلتِ قابل تنظیم، پیشرفتِ زنده، مرتب‌سازی بر پایه‌ی تأخیر
- جدول `scan_cache` در D1 برای نگه‌داریِ نتایج بین اسکن‌ها
- اعمالِ مستقیمِ برترین‌ها روی `cleanIPs` با برچسبِ `COLO-LOC`
- مسیرهای جدید: `GET /api/scan/cache` و `POST /api/scan/<candidates|probe|apply|clear>`

### اصلاح‌شده
- **بحرانی**: یک آکولادِ اضافه در تابعِ `renderSettings()` باعث **خطای نحویِ کل اسکریپتِ سمتِ کلاینت**
  می‌شد؛ در نتیجه داشبورد بارگیری می‌شد اما هیچ تعاملی (تب‌ها، کاربران، ذخیره‌ی تنظیمات) کار نمی‌کرد.
  این مشکل از نسخه‌ی 0.1.0 وجود داشت. هم‌اکنون یک تستِ خودکار نحویِ اسکریپتِ کلاینت را بررسی می‌کند.

### تغییر‌یافته
- تعداد تأییدیه‌های تست از ۸۴ به ۱۰۵ افزایش یافت
- `tools/preview.js` اکنون پیش‌نمایشِ ایستای تبِ اسکنر را هم می‌سازد (`preview/scanner-preview.html`)

### شناخته‌شده
- اندازه‌گیریِ اسکنر از **دیدِ لبه‌ی کلودفلر** است، نه از شبکه‌ی کاربر نهایی؛
  برای یافتنِ نزدیک‌ترین و سالم‌ترین دیتاسنتر مناسب است، اما جایگزینِ تست روی خودِ کلاینت نیست.
- `cf.resolveOverride` در شبیه‌سازِ محلی (Miniflare) بی‌اثر است، بنابراین تست‌ها فقط
  ساختارِ پاسخ را می‌سنجند، نه دسترسی‌پذیریِ واقعی را.

## [0.1.0] — ۲۰۲۶-۰۸-۳۰

نخستین انتشار عمومی.

### افزوده‌شده

**هسته شبکه**
- پروتکل **VLESS** روی WebSocket + TLS با پشتیبانی از early data
- پروتکل **Trojan** با احراز هویت بر پایه‌ی SHA-224 (پیاده‌سازی داخلی)
- خروجی مستقیم، یا از طریق **ProxyIP** و **SOCKS5**
- پشتیبانی از DNS روی UDP از طریق DoH
- کلید قطع اضطراری (Kill Switch)
- غیرفعال‌سازی خودکار کاربر پس از اتمام حجم یا سررسید

**مدیریت کاربران**
- کاربران نامحدود با لینک اشتراک و کد QR اختصاصی
- سهمیه‌ی حجمی، تاریخ انقضا، محدودیت دستگاه، یادداشت
- شمارش مصرف به‌صورت دسته‌ای و غیرهمزمان برای کاهش نوشتن روی D1

**تولید کانفیگ**
- خروجی Base64، **Clash**، **Sing-box** و متن خام
- قالب نام‌گذاری پویا: `{FLAG} {CITY} {COUNTRY} {PROTO} {NUM} {NAME} {HOST} {DATE}`
- تولید خودکار نود برای هر آی‌پی تمیز
- صفحه‌ی وضعیت برای هر کاربر
- **تولیدکننده QR کاملاً داخلی** (ISO/IEC 18004) بدون هیچ وابستگی خارجی

**پنل**
- داشبورد فارسی/انگلیسی با تم اختصاصی تخت جمشید، حالت روشن و تاریک
- نشست امضا‌شده با HMAC-SHA256 و کوکی HttpOnly/Secure/SameSite
- گذرواژه با SHA-256 و نمک تصادفی
- استتارِ صفحه‌ی اصلی
- گزارش رویدادها، پشتیبان‌گیری و بازیابی کامل به‌صورت JSON

**ربات تلگرام**
- `/status` `/users` `/add` `/del` `/reset` `/link` `/pause` `/resume` `/help`

**توسعه**
- معماری ماژولار در ۱۰ فایل با اسکریپت تجمیع
- ۸۴ تست یکپارچه روی Miniflare (شامل اتصال واقعی WebSocket)
- ۹ تست تطبیق QR با پیاده‌سازی مرجع

### اصلاح‌شده
- دوبار شمرده شدن ترافیکِ دریافتی در آمار مصرف
- بازنشانیِ ناخواسته‌ی گذرواژه هنگام بازیابی پشتیبان
- محاسبه‌ی نادرست مکان الگوهای هم‌تراز در QR از نسخه‌ی ۷ به بالا

### شناخته‌شده
- ترافیک UDPِ غیرـDNS (تماس صوتی/تصویری) روی Workers پشتیبانی نمی‌شود
- اسکنر آی‌پی تمیز هنوز درون‌پنل نیست *(در 0.2.0 افزوده شد)*

</div>
