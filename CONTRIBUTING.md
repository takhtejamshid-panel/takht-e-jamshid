<div dir="rtl">

# 🤝 راهنمای مشارکت در تخت جمشید

سپاس که وقت می‌گذارید. این پروژه با یک هدف ساده ساخته شده:
**کدِ خوانا و قابل‌اعتماد** در فضایی که بیشتر نمونه‌هایش بسته یا مبهم‌سازی‌شده‌اند.
لطفاً همین روح را حفظ کنید.

---

## پیش از شروع

- برای تغییرات بزرگ، اول یک Issue باز کنید تا درباره‌ی جهتِ کار توافق کنیم.
- برای رفع اشکالِ کوچک، مستقیم Pull Request بفرستید.

---

## راه‌اندازی محیط توسعه

```bash
git clone https://github.com/<نام‌کاربری>/takht-e-jamshid.git
cd takht-e-jamshid
npm install
npm test          # اجرای همه‌ی تست‌ها
```

### ساختار

```
src/          ماژول‌های منبع — همیشه اینجا را ویرایش کنید
_worker.js    خروجی تجمیع‌شده — هرگز دستی ویرایش نشود
build.js      تجمیع‌کننده
tests/        تست‌ها روی Miniflare
tools/        ابزارهای کمکی (ساخت پیش‌نمایش)
```

---

## گردش کار

```bash
git switch -c feature/نام-ویژگی
# ... ویرایش در src/ ...
node build.js        # تجمیع در _worker.js
npm test             # باید همه سبز باشد
git add .
git commit -m "توضیح روشنِ تغییر"
git push origin feature/نام-ویژگی
```

> ⚠️ CI بررسی می‌کند که `_worker.js` با `src/` هم‌خوان باشد.
> اگر `node build.js` را اجرا نکنید، ساخت شکست می‌خورد.

---

## استانداردهای کد

| مورد | قرارداد |
|---|---|
| زبان | JavaScript خام برای Worker؛ بدون وابستگی خارجی در `_worker.js` |
| سبک | تابع‌های کوچک، نام‌های توصیفی، کامنتِ فارسی برای بخش‌های غیربدیهی |
| تورفتگی | دو فضا |
| ویرگول‌گذاری | همیشه |
| رشته‌ها | ترجیحاً تک‌کوتیشن در کد worker |
| متغیرها | `const`/`let`؛ بدون `var` در کد جدید سمت سرور |

### اصول اختصاصی این پروژه

1. **هیچ وابستگی خارجی در خروجی نهایی.** هر ابزار لازم (مثل تولید QR یا SHA-224)
   باید داخل پروژه پیاده‌سازی شود.
2. **هیچ تماس شبکه‌ایِ پنهان.** کاربر باید دقیقاً بداند پنل با چه کسی صحبت می‌کند.
3. **هر ویژگیِ جدید تست دارد.** به‌ویژه هر چیزی که به تحلیل بسته یا احراز هویت ربط دارد.
4. **بدون مبهم‌سازی (obfuscation).** اگر قرار است چیزی مخفی بماند، آن را در
   تنظیمات بگذارید، نه در کد.

---

## تست‌ها

```bash
node tests/run.js      # ۸۴ تست یکپارچه روی Miniflare
node tests/test_qr.js  # ۹ تست تولید QR در برابر کتابخانه مرجع
npm test               # هر دو
```

تست‌های یکپارچه شامل یک **اتصال واقعی WebSocket** به یک سرور TCP محلی هستند؛
بنابراین تغییر در هسته‌ی پروکسی بی‌درنگ در تست منعکس می‌شود.

---

## پیامِ commit

- فارسی یا انگلیسی، فرقی ندارد؛ اما **روشن** باشد.
- از فعل امری استفاده کنید: «افزودن پشتیبانی از …»، «رفع دوبار‌شماری مصرف»
- اگر رفع اشکال است، شماره‌ی Issue را بیاورید: `رفع #12: ...`

---

## امنیت

اگر آسیب‌پذیری امنیتی پیدا کرده‌اید، **Issue عمومی باز نکنید.**
به [SECURITY.md](./SECURITY.md) مراجعه کنید.

---

## مجوز

با ارسال Pull Request می‌پذیرید که مشارکت شما تحت مجوز MIT منتشر شود.

</div>
